version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "file:/app/data/prod.db"
    env_file:
      - .env
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
